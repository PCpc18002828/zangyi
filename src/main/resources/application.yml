# 数据库连接设置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zangyi?useSSL=false&useUnicode=true&characterEncoding=utf8&
    username: root
    password: 123456

# Mybatis 配置
mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml,classpath:/mybatis/mapper/Extend/*.xml
logging:
  level:
    com.rainbow.webMall.mapper: debug